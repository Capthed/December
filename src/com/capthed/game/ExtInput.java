package com.capthed.game;

import com.capthed.abyss.component.GameComponent;

public class ExtInput extends GameComponent {

	private boolean a = true, b = true;
	
	public ExtInput() {
		super();
	}
	
	/*public void update() {
		if (Timer.getTimeRunning() >= 3 && a) {
			TestRun.scene.setEnabled(false);
			a = false;
			
		} else if (Timer.getTimeRunning() >= 7 && b) {
			TestRun.scene.setEnabled(true);
			b = false;
		}
	}*/
}
